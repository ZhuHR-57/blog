import { DPlayer, DPlayerOptions } from "../../types";
export default class Dplayer {
    constructor(src?: DPlayerOptions);
    src?: DPlayerOptions;
    player?: DPlayer;
    InitPlayer(): Promise<DPlayer | undefined>;
    DestroyPlayer(): void;
    AddOnEvent(on?: Record<string, (player: DPlayer, src: DPlayerOptions) => void>): void;
}
