import { Xgplayer as XGplayer, IPlayerOptions } from "../../types";
export default class Xgplayer {
    constructor(src?: IPlayerOptions);
    src?: IPlayerOptions;
    player?: XGplayer;
    InitPlayer(): Promise<XGplayer | undefined>;
    DestroyPlayer(): void;
    AddOnEvent(on?: Record<string, (player: XGplayer, src: IPlayerOptions) => void>): void;
}
