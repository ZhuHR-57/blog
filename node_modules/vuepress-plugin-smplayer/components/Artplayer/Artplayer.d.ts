import { Artplayer, ArtplayerOptions } from "../../types";
export default class ArtplayerVue {
    constructor(src?: ArtplayerOptions);
    src?: ArtplayerOptions;
    player?: Artplayer;
    InitPlayer(): Promise<Artplayer | undefined>;
    DestroyPlayer(): void;
    AddOnEvent(on?: Record<string, (player: Artplayer, src: ArtplayerOptions) => void>): void;
}
