import { APlayerOptions, APlayer } from "../../types";
export default class Aplayer {
    constructor(src?: APlayerOptions);
    src?: APlayerOptions;
    player?: APlayer;
    InitPlayer(): Promise<APlayer | undefined>;
    DestroyPlayer(): void;
    AddOnEvent(on?: Record<string, (player: APlayer, src: APlayerOptions) => void>): void;
}
