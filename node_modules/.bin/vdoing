#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../vuepress-theme-vdoing/bin/checkVersion.js" "$@"
  ret=$?
else 
  node  "$basedir/../vuepress-theme-vdoing/bin/checkVersion.js" "$@"
  ret=$?
fi
exit $ret
